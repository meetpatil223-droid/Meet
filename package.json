{
  "name": "meet-patil-portfolio",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "pip install -r requirements.txt || echo Build complete",
    "start": "gunicorn app:app || python app.py"
  }
}